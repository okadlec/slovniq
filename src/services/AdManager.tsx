import React from 'react';

interface AdBannerProps {
  isPro: boolean;
}

// AdMob je dočasně deaktivovaný — aktivovat až bude produkční AdMob App ID
export const AdBanner: React.FC<AdBannerProps> = ({ isPro }) => {
  return null;
};
